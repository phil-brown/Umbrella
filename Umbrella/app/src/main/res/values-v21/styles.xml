<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="PreferencesTheme" parent="android:Theme.Material.Light">
        <!-- Custom for Material Design-->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/action_bar_background</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/action_bar_background</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/action_bar_background</item>
    </style>

</resources>
